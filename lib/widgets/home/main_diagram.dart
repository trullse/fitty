import 'package:flutter/material.dart';

class MainDiagram extends StatelessWidget {
  const MainDiagram({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 400,
    );
  }
}
